# 2026-09-13 佩戴测试与点云显示

记录：run_20260913_200139。采集已正常停止。

- 2,118 帧，约 106.87 秒；原始记录有地图支持的跟踪帧为 1,076（约 50.8%）。没有轨迹真值，不能据此证明定位精度或相对上次的连续性改善。
- 稠密点云 150,526 点，完整 PLY 约 5.4 MB；网页均匀抽样显示 50,176 点。
- 166 组深度参与最终位姿建模，产出 38,702 个顶点、29,527 个三角面，后台构建约 1.59 秒。
- 实际浏览可见点云和网格，但表面仍呈碎片状，有明显散点；文件成功导出不等于已重建完整物体。需进一步检查立体深度质量和持续跟踪。

## 页面行为

结束采集后自动切换到回放并加载已有点云。优先读取非空稠密点云，缺失、空文件或格式不可用时退回稀疏点云。没有网格也能独立显示点云；两种点云都没有有效点时明确提示，不生成假数据。

点云采用独立参考视图，避免把历史在线融合的坐标直接叠到最终优化轨迹。勾选“显示物体表面模型”会切换为模型与最终轨迹；关闭点云可查看轨迹。点云最多预览 60,000 点，“下载完整点云 PLY”保留全部点。使用固定屏幕点大小，使远处点云仍可见。

[打开本次记录](http://192.168.4.124:8765/?run=run_20260913_200139)。旧页面需要刷新后加载新版。

## 验证

21 项 Python 测试和 JavaScript 轨迹测试通过。Chrome 实测本次点云自动加载、完整 PLY 下载、网格切换；另用没有表面模型的旧记录验证自动显示 56,936 个点。页面无 JavaScript 异常，390 px 宽度无横向溢出。未为此次网页修改重新开启摄像头。
